// Long-form About content per locale (rendered in the #about section).
// The text lives in src/data/about.json so the /admin panel can edit it.
import data from '../data/about.json';

export const about = data;
