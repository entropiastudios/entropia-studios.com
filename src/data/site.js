// Studio details shared by every page (addresses, email, socials).
// The values live in src/data/site.json so the /admin panel can edit them.
// analyticsId is the Google Analytics 4 Measurement ID, loaded only AFTER the
// visitor accepts analytics cookies (Consent Mode). Set to '' to disable.
import data from './site.json';

export const site = data;
